# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'MR_Operation' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MR_Operation
  pod 'MagicalRecord', :git => 'https://github.com/ShaoJenChen/MagicalRecord.git' , :branch => 'release/3.0'
end
